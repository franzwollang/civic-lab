# dullahan-web stub

Minimal stand-in for the sibling `dullahanUI` package used by the
Figma Make scaffold (`ClientPageProvider`, `ok`, remote handler types).

Replace with the real package when developing alongside `dullahanUI`.
This stub is enough for Civic Lab prototype install/build and editor API flows.
