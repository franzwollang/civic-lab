# Package-level pnpm settings live in package.json ("pnpm.onlyBuiltDependencies").
# Do not put JSON arrays in this file — pnpm parses .npmrc values as strings and
# onlyBuiltDependencies?.sort then throws.
